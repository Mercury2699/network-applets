Functions:

The bonus portion is not yet implemented. 

Design choices:

When updating topological view on each router, I keep the vertex pairs to be formatted as ((v1,v2),cost) and sorted so that v1 < v2. In order to avoid duplicate links added into the view. 

